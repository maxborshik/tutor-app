{
  "name": "Tutor App Dev Environment",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "backend",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vscode.vscode-typescript-next",
        "charliermarsh.ruff"
      ]
    }
  },
  "postCreateCommand": "uv sync --project backend && cd frontend && npm install" 
}
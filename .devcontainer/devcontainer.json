{
  "name": "Tutor-App-Debug",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "backend",
  "workspaceFolder": "/app",
  // REMOVE all features and extensions for this one test
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": ["ms-python.python"] // Only ONE extension
    }
  },
  "postCreateCommand": "echo 'Stable Baseline Reached'"
}